node_modules/
dist/
build/

# Playwright and Allure output — all generated, none of it belongs in git.
test-results/
playwright-report/
blob-report/
allure-results/
allure-report/
# The trend file, generated. Committing it would ship run data from whichever
# machine built the report last — noise to a reader, conflicts to a
# contributor. A real deployment keeps history in CI instead.
allure-history.json
# The json reporter, written only in CI for the on-demand callback.
results.json
.auth/

.env
.env.local
.env*
.DS_Store

# The leak checker word list — local only, never published.
# See the note at the top of scripts/check-leak.mjs.
.leakwords.json
